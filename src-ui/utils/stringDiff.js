const findStringDiff = (a, b) => {
    let diff = {
        op: '',
        content: '',
        start: {row: 0, col: 0},
        end: {row: 0, col: 0},
    };

    //TODO: Implement diff algorithm

    return diff;
}

const applyDiff = (a, diff) => {
    let b = a;

    // TODO: Implement apply diff algorithm

    return b;
}